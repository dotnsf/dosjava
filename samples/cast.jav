// NOTE: This file tests type casting (both implicit and explicit).
// Type casting is not yet fully implemented in the compiler.
// This file will be enabled in a future phase when casting is supported.

/*
class CastTest {
    public static void main() {
        // Test implicit int to long cast (widening)
        int i1 = 100;
        long l1 = i1;
        
        // Test implicit int to float cast (widening)
        int i2 = 10;
        float f1 = i2;
        
        // Test implicit long to float cast (widening)
        long l2 = 1000L;
        float f2 = l2;
        
        // Test multiple implicit casts
        int i3 = 50;
        long l3 = i3;
        float f3 = i3;
        
        // Test with expressions
        int i4 = 25;
        int i5 = 25;
        long l4 = i4 + i5;  // Result is implicitly cast to long
        
        // Verify results
        if (l1 == 100L && 
            f1 > 9.99f && f1 < 10.01f &&
            f2 > 999.9f && f2 < 1000.1f &&
            l3 == 50L && f3 > 49.99f && f3 < 50.01f &&
            l4 == 50L) {
            System.out.println("cast.jav worked correctly.");
        }
        
        return;
    }
}
*/

// Made with Bob
