/*
 * dosjava Configuration File for mTCP
 */

#ifndef CONFIG_H
#define CONFIG_H

#define MTCP_PROGRAM_NAME "dosjava"

// Disable heap checking BEFORE including GLOBAL.CFG
// This prevents false positives with malloc/free usage
#define UTILS_CHECK_HEAP (0)
#define UTILS_HEAP_CHECK_LEVEL (0)

// Include global mTCP configuration
#include "GLOBAL.CFG"

// Force disable heap checking AFTER including GLOBAL.CFG
#undef UTILS_CHECK_HEAP
#define UTILS_CHECK_HEAP (0)
#undef UTILS_HEAP_CHECK_LEVEL
#define UTILS_HEAP_CHECK_LEVEL (0)

// Local TCP/IP library options
#define COMPILE_ARP
// #define IP_FRAGMENTS_ON  // Disabled to save memory - IP fragmentation not needed
#define COMPILE_UDP
#define COMPILE_TCP
#define COMPILE_DNS
#define COMPILE_ICMP

// Override defaults for our application (reduced for memory constraints)
#undef TCP_MAX_SOCKETS
#define TCP_MAX_SOCKETS (2)

#undef PACKET_BUFFERS
#define PACKET_BUFFERS (8)

#undef TCP_MAX_XMIT_BUFS
#define TCP_MAX_XMIT_BUFS (4)

// TCP socket ring buffer size (increased for sockhelp.exe with Large model)
#undef TCP_SOCKET_RING_SIZE
#define TCP_SOCKET_RING_SIZE (4096)

#define TCP_LISTEN_CODE

#endif
